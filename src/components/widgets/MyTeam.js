import React from 'react';
import Widget from '../layout/Widget';

const ProjectTeam = () => {
  return (
    <Widget heading='Team [insert teamname]' className='team-component'>
      For some projects
    </Widget>
  );
};

export default ProjectTeam;
