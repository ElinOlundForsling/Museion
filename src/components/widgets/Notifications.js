import React from 'react';
import Widget from '../layout/Widget';

const Notifications = () => {
  return (
    <Widget heading='Notifications' className='notifications-component'>
      Where to turn stuff in ext
    </Widget>
  );
};

export default Notifications;
