import React from 'react';
import Widget from '../layout/Widget';

const Documents = () => {
  return (
    <Widget heading='Documents' className='documents-component'>
      Where to turn stuff in ext
    </Widget>
  );
};

export default Documents;
