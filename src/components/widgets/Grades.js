import React from 'react';
import Widget from '../layout/Widget';

const Grades = () => {
  return (
    <Widget heading='Grades' className='grades-component'>
      Recent grades
    </Widget>
  );
};

export default Grades;
