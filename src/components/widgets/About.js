import React from 'react';
import Widget from '../layout/Widget';

const About = () => {
  return (
    <Widget heading='About' className='about-component'>
      Where to turn stuff in ext
    </Widget>
  );
};

export default About;
